# Environment Variables Example
# Copy this file to .env and fill in your actual values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# Local Development (PostgreSQL)
DATABASE_URL=postgresql://postgres:local_password@localhost:5432/postgres?sslmode=disable
DIRECT_URL=postgresql://postgres:local_password@localhost:5432/postgres?sslmode=disable

# Production (Supabase)
# DATABASE_URL=postgresql://postgres:<PASSWORD>@aws-1-us-west-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true
# DIRECT_URL=postgresql://postgres:<PASSWORD>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require

# ===========================================
# AUTHENTICATION
# ===========================================

# Google OAuth (required for NextAuth)
# Get these from: https://console.cloud.google.com/
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (dev)
# 3. Add authorized redirect URI: https://yourdomain.com/api/auth/callback/google (prod)
AUTH_GOOGLE_ID=your_google_client_id_here
AUTH_GOOGLE_SECRET=your_google_client_secret_here

# NextAuth secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your_random_secret_key_here

# ===========================================
# ENVIRONMENT
# ===========================================

# Set to 'production' for production deployments
NODE_ENV=development

# ===========================================
# NOTES
# ===========================================

# For local development:
# - Use local PostgreSQL connection strings
# - Set NODE_ENV=development
# - Use localhost redirect URI for Google OAuth

# For production deployment:
# - Use Supabase pooler URL for DATABASE_URL (with pgbouncer=true)
# - Use Supabase direct URL for DIRECT_URL (for Prisma migrations)
# - Set NODE_ENV=production
# - Update Google OAuth redirect URI to your production domain
# - Generate a new AUTH_SECRET for production

# Prisma will use DIRECT_URL for migrations and DATABASE_URL for runtime queries
